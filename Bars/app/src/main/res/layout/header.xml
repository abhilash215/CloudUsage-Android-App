<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="190dp"
    android:gravity="bottom"
    android:id="@+id/navigation_header_container"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:background="@drawable/images_header">

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/imageView_h"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:src="@drawable/aboutme"
        app:civ_border_width="2dp"
        app:civ_border_color="#FF000000"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Abhilash"
        android:layout_below="@+id/imageView_h"
        android:textStyle="bold"
        android:textSize="17sp"
        android:id="@+id/text_name"
        android:textColor="#303185"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <TextView
        android:id="@+id/text_id"
        android:layout_width="wrap_content"
        android:layout_below="@+id/text_name"
        android:textSize="14sp"
        android:textColor="#44040d"
        android:layout_height="wrap_content"
        android:text="audayash@syr.edu"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

</RelativeLayout>
